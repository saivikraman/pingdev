## Prerequisites
*  -> code

Before running the project, make sure you have the following installed:

- Python
- Django

->Install Django
*pip install pipenv
*pipenv shell
Go to place where virtual environment is installed
*pip install django(in cmd)

-> Run server
change the location of cmd to the ping file 
*python manage.py runserver


